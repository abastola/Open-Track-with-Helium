<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta http-equiv="x-ua-compatible" content="ie=edge" />
  <title>OpenTrack with Helium</title>

  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css" />
  <!-- Google Fonts Roboto -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
  <!-- MDB -->
  <link rel="stylesheet" href="/stylesheets/mdb.min.css" />

  <link rel="stylesheet" href="/stylesheets/noty.min.css">
  <script src="/javascripts/noty.min.js"></script>

  <!-- Custom styles -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.min.js"
    integrity="sha512-d9xgZrVZpmmQlfonhQUvTR7lMPtO7NkZMkA0ABN3PHCbKA5nqylQ/yWlFAyY6hYgdF1Qh6nYiuADWwKB4C2WSw=="
    crossorigin="anonymous"></script>


  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css"
    integrity="sha512-hoalWLoI8r4UszCkZ5kL8vayOGVae1oxXe/2A4AO6J9+580uKHDO3JdHb7NzwwzK5xr/Fs0W40kiNHxM9vyTtQ=="
    crossorigin="" />

  <script src="https://unpkg.com/leaflet@1.8.0/dist/leaflet.js"
    integrity="sha512-BB3hKbKWOc9Ez/TAwyWxNXeoV9c1v6FIeYiBieIWkpLjauysF18NzgR1MBNBXf8/KABdlkX68nAhlwcDFLGPCQ=="
    crossorigin=""></script>

  <script src="/javascripts/qrcode.min.js"></script>

  <script src="/javascripts/leaflet-ant-path.js"> </script>

  <link rel="stylesheet" href="/stylesheets/admin.css" />


  <style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      user-select: none;
    }

    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }

    .b-example-divider {
      height: 3rem;
      background-color: rgba(0, 0, 0, .1);
      border: solid rgba(0, 0, 0, .15);
      border-width: 1px 0;
      box-shadow: inset 0 .5em 1.5em rgba(0, 0, 0, .1), inset 0 .125em .5em rgba(0, 0, 0, .15);
    }

    .b-example-vr {
      flex-shrink: 0;
      width: 1.5rem;
      height: 100vh;
    }

    .bi {
      vertical-align: -.125em;
      fill: currentColor;
    }

    .nav-scroller {
      position: relative;
      z-index: 2;
      height: 2.75rem;
      overflow-y: hidden;
    }

    .nav-scroller .nav {
      display: flex;
      flex-wrap: nowrap;
      padding-bottom: 1rem;
      margin-top: -1px;
      overflow-x: auto;
      text-align: center;
      white-space: nowrap;
      -webkit-overflow-scrolling: touch;
    }
  </style>
</head>

<body>

  <!--Main Navigation-->
  <header>
    <!-- Sidebar -->
    <nav id="sidebarMenu" class="collapse d-lg-block sidebar collapse bg-dark topHeader">
      <div class="position-sticky">
        <div class="list-group list-group-flush mx-3 mt-4">
          <a href="#" class="list-group-item list-group-item-action py-2 ripple" aria-current="true">
            <i class="fas fa-tachometer-alt fa-fw me-3"></i><span>Dashboard</span>
          </a>
          <a href="https://www.iotoffgrid.com/" class="list-group-item list-group-item-action py-2 ripple active">
            <i class="fas fa-chart-area fa-fw me-3"></i><span>Iot Off-Grid</span>
          </a>
          <a href="https://www.helium.com/" class="list-group-item list-group-item-action py-2 ripple"><i
              class="fas fa-lock fa-fw me-3"></i><span>Helium</span></a>
          <a href="http://www.helium.foundation/" class="list-group-item list-group-item-action py-2 ripple"><i
              class="fas fa-chart-line fa-fw me-3"></i><span>Helium Foundation</span></a>
          <a href="https://dewi.org/" class="list-group-item list-group-item-action py-2 ripple">
            <i class="fas fa-chart-pie fa-fw me-3"></i><span>DeWi</span>
          </a>
          <a href="https://rakwireless.kckb.st/50a28dfa" class="list-group-item list-group-item-action py-2 ripple"><i
              class="fas fa-chart-bar fa-fw me-3"></i><span>Buy WisNode TrackIt RAK2171</span></a>
          <a href="#" class="list-group-item list-group-item-action py-2 ripple">
            <div id="qrcode">
            </div>
          </a>

        </div>
      </div>
    </nav>
    <!-- Sidebar -->

    <!-- Navbar -->
    <nav id="main-navbar" class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top topHeader">
      <!-- Container wrapper -->
      <div class="container-fluid">
        <!-- Toggle button -->
        <button class="navbar-toggler" type="button" data-mdb-toggle="collapse" data-mdb-target="#sidebarMenu"
          aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
          <i class="fas fa-bars"></i>
        </button>

        <!-- Brand -->
        <a class="navbar-brand" href="/">
          OpenTrack with Helium
        </a>
      </div>
      <!-- Container wrapper -->
    </nav>
    <!-- Navbar -->
  </header>
  <!--Main Navigation-->

  <!--Main layout-->
  <main style="margin-top: 58px">
    <div class="card m-4 mainCard">
      <div class="container-fluid pt-4">
        <div class="btn-toolbar mb-2 mb-md-0">
          <button type="button" class="btn btn-dark m-1" onclick="copyURLToClipboard()">Share</button>
          <button type="button" class="btn btn-dark m-1" onclick="exportToExcel();">Export</button>
          <div class="dropdown">
            <button class="btn btn-dark dropdown-toggle m-1" type="button" id="dropdownMenuButton"
              data-mdb-toggle="dropdown" aria-expanded="false">
              Show
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
              <li>
                <a class="dropdown-item" href="#" onclick="RefreshLastLocation(50)">Last 50</a>
              </li>
              <li>
                <a class="dropdown-item" href="#" onclick="RefreshLastLocation(75)">Last 75</a>
              </li>
              <li>
                <a class="dropdown-item" href="#" onclick="RefreshLastLocation(100)">Last 100</a>
              </li>
              <li>
                <a class="dropdown-item" href="#" onclick="RefreshLastLocation()">All</a>
              </li>
            </ul>
          </div>
        </div>
      </div>

      <div class="container-fluid pt-4">
        <!-- Section: Main chart -->
        <section class="mb-4">
          <div class="">
            <div class="card-header p-0">
              <div class="w-100 card-img-top" id="map" width="900" height="380"></div>
            </div>
            <div class="card-body">
              <strong>OpenTrack is built by <a href="https://www.iotoffgrid.com/">IoT Off-Grid</a> with support from <a
                  href="https://www.helium.foundation/">Helium Foundation</a> | Improve OpenTrack on <a
                  href="https://github.com/abastol1/tracker">Github</a></strong>
            </div>
          </div>
        </section>
        <!-- Section: Main chart -->
      </div>
    </div>

  </main>
  <!--Main layout-->
  <!-- MDB -->
  <script type="text/javascript" src="/javascripts/mdb.min.js"></script>
  <!-- Custom scripts -->
  <script type="text/javascript" src="/javascripts/admin.js"></script>

  <script type="text/javascript">
    new QRCode(document.getElementById("qrcode"), {
      text: window.location.href,
      width: 128,
      height: 128,
      colorDark: "#000000",
      colorLight: "#ffffff",
      correctLevel: QRCode.CorrectLevel.H
    });
    var locations = '<%- JSON.stringify(Response.locations) %>';
    locations = JSON.parse(locations);

    var focusedLocation = [11.206051, 122.447886];
    if (locations.length > 0) {
      focusedLocation = [locations[locations.length - 1][2], locations[locations.length - 1][3]];
    }

    var map = L.map('map').setView(focusedLocation, 14);
    mapLink = '<a href="http://openstreetmap.org">OpenStreetMap</a>';
    RefreshLastLocation(1);

    function RefreshMarkers() {
      map.eachLayer((layer) => { layer.remove(); });
      L.tileLayer('http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '&copy; ' + mapLink + ' Contributors',
        maxZoom: 18,
      }).addTo(map);
    }

    function ShowLastLocation(num) {
      var n = num === undefined ? locations.length : Math.min(num, locations.length);
      var latLongs = [];
      for (var i = locations.length - n; i < locations.length; i++) {
        marker = new L.circleMarker([locations[i][2], locations[i][3]])
          .bindPopup(locations[i][0])
          .addTo(map);

        latLongs.push([locations[i][2], locations[i][3]]);
      }

      var polyline = L.polyline(latLongs).addTo(map);
      var antPolyline = L.polyline.antPath(latLongs).addTo(map);
    }

    function RefreshLastLocation(num) {
      RefreshMarkers();
      ShowLastLocation(num);
    }

    var exportToExcel = () => {
      var element = document.createElement('a');
      var csv = "";
      csv = "Timestamp, Latitude, Longitude \n";
      locations.forEach((curr, i, arr) => {
        csv += curr[0] + "," + curr[2] + "," + curr[3] + "\n";
      });

      element.setAttribute('href', 'data:text/plain;charset=utf-8,' + encodeURIComponent(csv));
      element.setAttribute('download', "OpenTrakerData.csv");
      element.style.display = 'none';
      document.body.appendChild(element);
      element.click();
      document.body.removeChild(element);
    }
  </script>
</body>

</html>